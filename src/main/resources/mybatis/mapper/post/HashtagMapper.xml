<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.sys4u.spring.instagram.core.repository.post.HashtagRepository">
  <select id="selectHashtagByPostNo" resultType="String">
  	SELECT NAME FROM HASHTAG WHERE POST_NO = #{no}
  </select>
  <insert id="insert" parameterType="Hashtag" useGeneratedKeys="false">
  	INSERT INTO HASHTAG(NO
  					, POST_NO
  					, NAME)
		  	VALUES (SEQ_HASHTAG.NEXTVAL
		  			, SEQ_POST.CURRVAL
		  			, #{hashtag.name})
  </insert>
</mapper>